<?xml version='1.0' encoding='utf-8'?>
<rss xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0">
  <channel>
    <title>Simone Lungarella</title>
    <link>https://simone-lungarella.github.io/</link>
    <description>Notes, experiments, and thoughts.</description>
    <language>en</language>
    <lastBuildDate>Thu, 18 Jun 2026 06:24:26 +0000</lastBuildDate>
    <item>
      <title>Stack Overflow's merits</title>
      <link>https://simone-lungarella.github.io/articles/stackoverflow-merits.html</link>
      <guid isPermaLink="true">https://simone-lungarella.github.io/articles/stackoverflow-merits.html</guid>
      <pubDate>Thu, 18 Jun 2026 00:00:00 +0000</pubDate>
      <description>Knowing how to ask questions is quite handy in LLM era</description>
      <content:encoded>&lt;h2 id="stack-overflow-is-a-painful-experience"&gt;Stack Overflow is a
painful experience&lt;/h2&gt;
&lt;p&gt;Stack Overflow is known to be a painful experience where emotionless
people criticize your way to pose questions and downvote your low-effort
posts down to the abyss.&lt;/p&gt;
&lt;p&gt;This harsh approach can be quite demotivating but I’d say it’s rather
efficient. Every new user who finds himself in the uncomfortable
situation where multiple users denigrate the way he posed the question
rather than answering it, will often put more effort on the following
questions.&lt;/p&gt;
&lt;p&gt;Sometimes they will never post again but those who resist, will often
make sure to properly document their successive questions and put some
effort on trying to solve their problem alone first.&lt;/p&gt;
&lt;p&gt;I get it, it is upsetting when a question is marked as irrelevant or
duplicated and closed without an answer when you are just starting to
code and have many doubts. However, if you didn’t make any research and
didn’t try to solve your own problem before reaching for help maybe it’s
sane that someone addresses your mistakes and invites you to &lt;a
href="https://en.wikipedia.org/wiki/RTFM"&gt;read the friendly
manual&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id="the-merits"&gt;The merits&lt;/h2&gt;
&lt;p&gt;I believe that the Stack Overflow experience is not as bad as it is
pictured and in fact, it can help you a lot to improve your
communication skills which is the basis of a successful software
engineer.&lt;/p&gt;
&lt;p&gt;This is even more true today, where a good prompt with enough context
and a clear goal can make the difference between a good AI generated
answer and a bad one. I don’t love the &lt;code&gt;Prompt Engineer&lt;/code&gt;
narrative, but it is undeniable that prompts can be good and bad, and a
good prompt will increase a lot the token efficiency of a model
providing better results with less tokens spent and less wasted
time.&lt;/p&gt;
&lt;p&gt;It’s obvious that good writing skills are useful in many contexts,
however, I’ve seen many not-so-junior developers having great difficulty
to precisely express themselves.&lt;/p&gt;
&lt;p&gt;Let’s consider, for example, a task definition: in some cases it is
important to be as precise one can be so to be sure that every behaviour
is aligned with the request. Leaving too many unspecified conditions or
omitting key elements is unfortunately very common and if Stack Overflow
user base highlights those problems, the AI will implicitly assume
everything and provide a convincing answer that often is correct from
the standpoint of how the question is posed but ultimately incorrect
when confronted with the original request.&lt;/p&gt;
&lt;h2 id="conclusion"&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;Stack Overflow is not the only way to improve your writing skills, of
course, but is the only method that seems to be so hated. This is
perhaps due to the overextended gamification mechanics that make certain
threads pretty quickly toxic, but the platform is full of nice
discussions and valuable insights and I don’t think it deserves all the
hate.&lt;/p&gt;
</content:encoded>
    </item>
    <item>
      <title>Effective Snapshot Testing</title>
      <link>https://simone-lungarella.github.io/articles/effective-snapshot-testing.html</link>
      <guid isPermaLink="true">https://simone-lungarella.github.io/articles/effective-snapshot-testing.html</guid>
      <pubDate>Sun, 17 May 2026 00:00:00 +0000</pubDate>
      <description>The power of snapshot testing and how to make them effective</description>
      <content:encoded>&lt;p&gt;Snapshot tests verify the &lt;em&gt;entire state&lt;/em&gt; of a system rather
than individual assertions. This significantly reduces the effort
required to write tests, as a single snapshot represents the expected
outcome.&lt;/p&gt;
&lt;p&gt;Keeping in mind that a snapshot test should be as narrow as it can be
so that any changes to the snapshot are easily reviewed, let’s focus on
a simple &lt;code&gt;java&lt;/code&gt; example:&lt;/p&gt;
&lt;pre class="java"&gt;&lt;code&gt;@Test
@Sql(scripts = &amp;quot;classpath:fixture/io2_data.sql&amp;quot;)
void computes_kpis_from_db_state() {
    var result = kpiService.calculateKpis(IO2, ALL);
    // Verify snapshot
    JsonApprovals.verifyJson(new Gson().toJson(result));
}&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In this example, a &lt;code&gt;fixture&lt;/code&gt; provides data preparation for
a test and the snapshot represents the expected full output state after
logic execution. Even in complex systems, a snapshot should be very
small; otherwise it becomes hard to maintain.&lt;/p&gt;
&lt;hr /&gt;
&lt;p&gt;One important thing to consider is to make sure that the final
snapshot is deterministic to avoid &lt;em&gt;flaky&lt;/em&gt; tests. In the example
where we have &lt;code&gt;JSON&lt;/code&gt; snapshot, a developer should:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Remove inconsistent fields such as random &lt;code&gt;UUID&lt;/code&gt;s;&lt;/li&gt;
&lt;li&gt;Remove or normalize dates, floating point numbers, etc.;&lt;/li&gt;
&lt;li&gt;Order each field in the same way so to have a result that can be
diffed with the previous test iteration;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre class="java"&gt;&lt;code&gt;@Test
@Sql(scripts = &amp;quot;classpath:fixture/io2_data.sql&amp;quot;)
void computes_kpis_from_db_state() {

    // Enforce time usage to have deterministic dates
    try (WithTimeZone tz = new WithTimeZone(&amp;quot;UTC&amp;quot;)) {

        var result = kpiService.calculateKpis(IO2, ALL);
        var normalizedJson = new Gson().toJson(normalize(result));

        JsonApprovals.verifyJson(normalizedJson);
    }
}&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A risk with tests like this, is that the simplicity of the assertion
makes it easy to correct when a test starts failing, for that reason I
suggest to use classic assertions to capture broader concepts that
should remain &lt;code&gt;true&lt;/code&gt; for the particular test.&lt;/p&gt;
&lt;pre class="java"&gt;&lt;code&gt;@Test
@Sql(scripts = &amp;quot;classpath:fixture/io2_data.sql&amp;quot;)
void computes_kpis_from_db_state() {

    // Enforce time usage to have deterministic dates
    try (WithTimeZone tz = new WithTimeZone(&amp;quot;UTC&amp;quot;)) {

        var result = kpiService.calculateKpis(IO2, ALL);
        var normalizedJson = new Gson().toJson(normalize(result));

        assertNull(result.getError());
        assertEquals(100, result.getProgressPercent());

        JsonApprovals.verifyJson(normalizedJson);
    }
}&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This does mitigate the risk but does not prevent developers from
updating snapshots without fully understanding the change; however, if
you are serious about programming, a good approach to this problem is to
treat failing snapshots as failing code and not replace them whenever
code changes make test fail. For that, one can leverage tools such as
&lt;code&gt;jq&lt;/code&gt; which is useful to compare two &lt;code&gt;JSON&lt;/code&gt;
files.&lt;/p&gt;
&lt;p&gt;For example, you can normalize and diff snapshots locally:&lt;/p&gt;
&lt;pre class="bash"&gt;&lt;code&gt;diff &amp;lt;(jq &amp;#39;del(.timestamp) | sort_keys&amp;#39; received.json) \
     &amp;lt;(jq &amp;#39;del(.timestamp) | sort_keys&amp;#39; approved.json)&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This approach makes any change intentional and specific so that any
code review can treat it just like any other piece of logic.&lt;/p&gt;
&lt;h2 id="other-readings"&gt;Other readings&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a
href="https://giacomocavalieri.me/writing/testing-can-be-fun-actually"&gt;Testing
can be fun, actually&lt;/a&gt; by Giacomo Cavalieri;&lt;/li&gt;
&lt;li&gt;&lt;a
href="https://github.com/approvals/ApprovalTests.Java/blob/master/approvaltests/docs/README.md"&gt;Snapshot
testing in Java&lt;/a&gt; with ApprovalsTest;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded>
    </item>
  </channel>
</rss>